{
  "name": "autocare360",
  "version": "1.0.0",
  "private": true,
  "description": "AutoCare360 — Complete vehicle maintenance, simplified. MERN SaaS for tracking maintenance, expenses, documents and reminders across every vehicle type.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
